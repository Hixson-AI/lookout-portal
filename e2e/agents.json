{
  "_meta": {
    "version": 4,
    "updated": "2026-04-19",
    "note": "Per-dim agent config. Mutated by reflect.mjs after each rubric run based on score deltas."
  },
  "D1": {
    "model": "openrouter/anthropic/claude-sonnet-4.6",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/components/workflow/FlowCanvas.tsx. Make exactly three changes: (1) Add `import { MiniMap } from '@xyflow/react'` to the existing @xyflow/react import line. (2) Inside the JSX returned by the component that renders `<ReactFlow ...>`, add `<MiniMap />` as a direct child of `<ReactFlow>`, placed after the last existing child element. (3) In the TriggerNode component (or wherever node styles/classNames are set for trigger-type nodes), add or merge `bg-indigo-50 border-indigo-400` into the className. (4) Confirm that `fitView` is called inside a callback or useEffect that runs after `setRfNodes` completes — if it is already called after setRfNodes, leave it; if not, move the fitView call so it executes in a subsequent effect or callback. Do not edit any other file.",
    "notes": "Empty patch with gemini-2.5-pro twice. Switching to claude-sonnet; decomposed into 4 numbered atomic changes with exact import syntax and placement anchors."
  },
  "D2": {
    "model": "openrouter/google/gemini-2.5-pro",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/components/workflow/DataMappingPanel.tsx. On the source field <code> element change class 'truncate' to 'break-all'. Set the left column min-h-[280px] overflow-y-auto.",
    "notes": "CSS tweak — gemini"
  },
  "D3": {
    "model": "openrouter/anthropic/claude-sonnet-4.6",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/components/workflow/StepConfigPanel.tsx. Inside the HttpRequestConfig component function, make exactly two JSX reordering changes. Change 1 — move Auth block before Headers block: find the JSX div or section that contains an <select> for auth type AND an <input> for credentials (this is the Auth block); cut the entire Auth block and paste it so it appears immediately before the JSX div or section whose label or heading text contains the word 'Headers'. Change 2 — unconditional Body textarea: find the JSX that renders a <textarea> for the request body; if that textarea is wrapped in a conditional expression such as `{method === 'POST' && (...)}` or `{['POST','PUT'].includes(method) && (...)}`, remove only the outer conditional wrapper so the textarea (and its surrounding label/wrapper div) renders unconditionally. Do not add, remove, or reorder any other JSX. Do not edit any other file.",
    "notes": "kimi-k2 produced only a blank line addition. Switching to claude-sonnet; rewritten to describe each change as a cut-and-paste or wrapper-removal operation with precise structural anchors."
  },
  "D4": {
    "model": "openrouter/anthropic/claude-sonnet-4.6",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/components/workflow/FlowCanvas.tsx. Inside the TriggerNode component function only, make exactly two changes: (1) In the import section at the top of the file, add `Zap` to the existing `lucide-react` import (e.g. change `import { X } from 'lucide-react'` to `import { X, Zap } from 'lucide-react'`); if lucide-react is not yet imported add `import { Zap } from 'lucide-react'`. Then, in the TriggerNode JSX, find the element that renders the trigger's label text and insert `<Zap className='h-3 w-3 inline mr-1' />` as a sibling immediately before that text node or text-rendering element. (2) In TriggerNode's JSX, find where the webhook URL is displayed (look for a string like '/webhooks/' or a prop named webhookUrl). Replace that rendered value with the expression: `{webhookUrl ? webhookUrl : '/webhooks/{id} — save to activate'}`. Do not touch any other component, hook, or file.",
    "notes": "Empty patch with both gemini and kimi-k2. Switching to claude-sonnet; each edit now specifies exact import mutation syntax and a concrete ternary expression to insert."
  },
  "D5": {
    "model": "openrouter/google/gemini-2.5-pro",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/components/workflow/StepConfigPanel.tsx AND src/components/workflow/FlowCanvas.tsx. FILE 1 — StepConfigPanel.tsx: Inside the StepConfigPanel component, (a) add `const [testResult, setTestResult] = React.useState<unknown>(null);` alongside the other useState declarations near the top of the component body. (b) After the closing tag of the config form (the last </div> or </form> that wraps the per-step config fields), insert this JSX block verbatim: `<button type='button' className='mt-2 px-3 py-1 text-xs rounded bg-blue-600 text-white' onClick={() => api.testStep(tenantId, appId, step).then(setTestResult)}>Test Step</button>{testResult && <pre className='text-xs overflow-auto mt-2'>{JSON.stringify(testResult, null, 2)}</pre>}`. FILE 2 — FlowCanvas.tsx: Inside the WorkflowNode component, (a) ensure the outermost wrapper div's className includes the word `relative` — if it already does, leave it; if not, append `relative` to the className string. (b) Add `{data.hasValidationError && <span className='absolute top-1 right-1 h-2 w-2 rounded-full bg-red-500 animate-pulse' />}` as the very last child inside that outermost wrapper div. Do not edit any other file.",
    "notes": "Empty patch with kimi-k2 twice. Switching to gemini-2.5-pro; rewrote with verbatim JSX snippets for each insertion point and explicit file-by-file structure."
  },
  "D6": {
    "model": "openrouter/anthropic/claude-sonnet-4.6",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/pages/AppBuilder.tsx. Make exactly two changes: (1) Near the top of the AppBuilder component body (alongside other useState calls), add: `const [helpOpen, setHelpOpen] = React.useState(false);`. Add `HelpCircle` to the existing lucide-react import line. In the canvas card's CardHeader JSX, insert `<button type='button' onClick={() => setHelpOpen(h => !h)}><HelpCircle className='h-4 w-4' /></button>` as the last child inside CardHeader. On the canvas card's outermost wrapper div, add the class `relative` if it is not already present. Immediately after the CardHeader closing tag (but still inside the canvas card div), insert: `{helpOpen && <div className='absolute inset-0 z-10 bg-white/90 p-4'><p>Settings panel: configure selected step.</p><p>Canvas panel: visual workflow.</p><p>Catalog panel: add new steps.</p></div>}`. (2) Find the empty-state overlay (rendered when steps.length === 0) — do not change its condition or markup. Do not edit any other file.",
    "notes": "Empty patch with gemini and kimi-k2. Switching to claude-sonnet; each JSX insertion is now written as a literal string to copy in, with explicit sibling/child placement anchors."
  },
  "D7": {
    "model": "openrouter/anthropic/claude-sonnet-4.6",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/pages/AppBuilder.tsx. Make exactly two changes (the third change from the previous round is already applied — do NOT re-apply the Save button text change): (1) Find every <CardTitle> element in this file and add className='font-semibold' to it (or append 'font-semibold' to an existing className string). (2) Find the step catalog item render — the JSX that maps over catalog items — and locate the icon wrapper div; change whichever gap class it has to gap-2.5. Do not alter any other JSX, logic, or styling.",
    "notes": "Previous patch removed the ⌘S span (partially correct) but score stayed at 4. Score goal is improvement on font-semibold and gap changes. Keeping claude-sonnet; removing the already-done Save change and narrowing to only the two remaining changes."
  },
  "D8": {
    "model": "openrouter/google/gemini-2.5-pro",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/pages/AppBuilder.tsx. Perform these exact className string replacements and nothing else: (1) In the outer grid container div, change the substring `lg:grid-cols-12` to `md:grid-cols-12`. (2) In the Settings panel child div, change `lg:col-span-3` to `md:col-span-3`. (3) In the Canvas panel child div, change `lg:col-span-5` to `md:col-span-5`. (4) In the Catalog panel child div, change `lg:col-span-4` to `md:col-span-4`. These are purely textual substitutions of Tailwind class names — do not alter any logic, event handlers, or other markup. Do not edit any other file.",
    "notes": "Empty patch with claude-sonnet and kimi-k2. Switching to gemini-2.5-pro; prompt reduced to bare minimum — four exact before→after string replacements only."
  },
  "D9": {
    "model": "openrouter/moonshotai/kimi-k2",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/pages/AppBuilder.tsx. Inside the Secrets card section of AppBuilder, make exactly two changes: (1) Find the conditional that gates rendering of the Add Secret form (the group containing a key text input, a password input, and an Add button) — this condition likely checks `requiredSecrets.length` or similar. Remove only that outer conditional wrapper so the Add Secret form always renders regardless of requiredSecrets length. (2) Find where saved secrets are listed (likely a `.map()` over a secrets array) and change the rendered element for each secret from whatever it currently is to `<span key={secret.key} className='bg-green-100 text-green-800 text-xs px-2 py-0.5 rounded-full'>{secret.key}</span>`. Do not modify any other section of this file or any other file.",
    "notes": "Empty patch with gemini-2.5-pro. Switching to kimi-k2; added explicit badge className and described the conditional-removal as 'remove only that outer conditional wrapper'."
  },
  "D10": {
    "model": "openrouter/google/gemini-2.5-pro",
    "temperature": 0.2,
    "prompt": "Edit ONLY: src/pages/AppBuilder.tsx. Make exactly three additions to the AppBuilder component: (1) Autosave badge — find the Save button JSX; immediately adjacent to it (before or after, on the same line or as a sibling), insert `{currentAppId && <span className='text-xs text-green-600 font-medium'>Autosave on</span>}`. (2) Undo stack — in the component body add: `const undoStack = React.useRef<any[]>([]);` and a `React.useEffect(() => { undoStack.current = [JSON.parse(JSON.stringify(workflow)), ...undoStack.current].slice(0, 10); }, [workflow]);`. (3) Undo button — add `Undo2` to the lucide-react import. In the header JSX, insert `<button type='button' disabled={undoStack.current.length === 0} onClick={() => { const prev = undoStack.current.shift(); if (prev) setWorkflow(prev); }}><Undo2 className='h-4 w-4' /></button>`. Do not modify any other file.",
    "notes": "Empty patch with kimi-k2. Switching to gemini-2.5-pro; each of the three additions is now a self-contained literal code snippet to insert, leaving nothing to inference."
  }
}
